<?xml version="1.0" encoding="UTF-8"?>
<!--
  Logback config for HQL tester tests.
  HqlTester logger is always INFO so SQL output is always visible.
  Hibernate SQL loggers are set to DEBUG so the generated SQL also appears
  in the standard Hibernate log channel, in addition to the inspector capture.
-->
<configuration>
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss} %-5level %logger{20} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Always show HQL tester output regardless of root level -->
    <logger name="HqlTester" level="INFO" additivity="false">
        <appender-ref ref="CONSOLE"/>
    </logger>

    <!-- Optional: also show Hibernate-generated SQL in the log -->
    <logger name="org.hibernate.SQL" level="DEBUG"/>
    <logger name="org.hibernate.orm.jdbc.bind" level="TRACE"/>

    <root level="WARN">
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration>
